---
phase: 02-batch-processing-reporting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pill_counter/batch.py
  - pill_counter/cli.py
  - tests/test_cli.py
autonomous: true
requirements:
  - OUT-03
  - OUT-04
  - ERR-03
  - DBG-03

must_haves:
  truths:
    - "User runs pill-counter on a folder and sees a progress bar during processing"
    - "After processing, report.json exists in the output directory"
    - "report.json contains total_pills, total_images, and results array matching D-01 shape"
    - "Each result in report.json has filename, count, status, centers, and optional error_msg"
    - "Terminal output (per-image counts and summary) still prints alongside JSON file generation"
    - "CLI exits 0 on all success, 2 on partial failure, 1 on total failure"
  artifacts:
    - path: "pill_counter/batch.py"
      provides: "process_folder with progress parameter"
      contains: "tqdm"
    - path: "pill_counter/cli.py"
      provides: "JSON report construction and writing"
      contains: "report.json"
    - path: "tests/test_cli.py"
      provides: "Tests for progress parameter and JSON report output"
      contains: "report"
  key_links:
    - from: "pill_counter/cli.py"
      to: "pill_counter/batch.py"
      via: "process_folder(progress=True)"
      pattern: "process_folder.*progress"
    - from: "pill_counter/cli.py"
      to: "output_dir/report.json"
      via: "json.dumps + write_text"
      pattern: "report\\.json"
---

<objective>
Add JSON report generation and progress bar to the batch pill-counting CLI.

Purpose: Phase 1 delivered the core CV pipeline and CLI. Phase 2 wraps it with structured reporting (OUT-03) and progress feedback (DBG-03). Requirements OUT-04 (--output flag) and ERR-03 (exit codes) were already implemented in Phase 1 and need no changes.

Output: Modified batch.py (progress bar), modified cli.py (JSON report), updated tests.
</objective>

<execution_context>
@/Users/danhtran/myData/src_learn/pill-counter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danhtran/myData/src_learn/pill-counter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-batch-processing-reporting/02-CONTEXT.md
@.planning/phases/02-batch-processing-reporting/02-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add progress bar to batch processing (DBG-03)</name>
  <files>pill_counter/batch.py, tests/test_cli.py</files>
  <read_first>
    - pill_counter/batch.py — current process_folder loop at line 86
    - tests/test_cli.py — existing batch test patterns (TestProcessFolder class)
    - pyproject.toml — tqdm>=4.67.0 already declared at line 13
  </read_first>
  <behavior>
    - Test: process_folder() accepts progress=False parameter (backward compatible, no crash)
    - Test: process_folder(progress=True) returns same results as progress=False
    - Test: scan_images, process_single_image, process_folder all still pass existing tests unchanged
  </behavior>
  <action>
    In `pill_counter/batch.py`:
    1. Add `from tqdm import tqdm` import at top (line 4 area).
    2. Change `process_folder` signature from `def process_folder(input_dir: Path, output_dir: Path) -> list[dict]:` to `def process_folder(input_dir: Path, output_dir: Path, progress: bool = False) -> list[dict]:`.
    3. Replace the loop at lines 86-88:
       ```python
       for img_path in images:
           result = process_single_image(img_path, output_dir)
           results.append(result)
       ```
       with:
       ```python
       iterator = tqdm(images, desc="Processing", unit="img") if progress else images
       for img_path in iterator:
           result = process_single_image(img_path, output_dir)
           results.append(result)
       ```

    In `tests/test_cli.py`:
    4. Add a new test class `TestProcessFolderProgress` with:
       - `test_progress_false_returns_same_results(tmp_path)`: Create 2 test images, call process_folder with progress=False, assert len(results)==2.
       - `test_progress_true_returns_same_results(tmp_path)`: Same setup, call with progress=True, assert identical result structure (filename, count, status keys present).
       - `test_progress_parameter_backward_compatible(tmp_path)`: Call process_folder(tmp_path, out_dir) without progress kwarg — must not raise TypeError.

    Do NOT change process_single_image or scan_images signatures. Do NOT change the return type of process_folder.
  </action>
  <verify>
    <automated>cd /Users/danhtran/myData/src_learn/pill-counter && python -m pytest tests/test_cli.py::TestProcessFolderProgress -x -v</automated>
  </verify>
  <acceptance_criteria>
    - process_folder() has progress: bool = False parameter in its signature
    - process_folder(progress=True) wraps image loop with tqdm
    - process_folder(progress=False) and process_folder() behave identically to before
    - All existing tests in TestProcessFolder still pass unchanged
    - New tests verify progress parameter works without changing return shape
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add JSON report generation to CLI (OUT-03)</name>
  <files>pill_counter/cli.py, tests/test_cli.py</files>
  <read_first>
    - pill_counter/cli.py — current main() at lines 49-76, process_folder call at line 50
    - tests/test_cli.py — existing TestCLI class patterns
    - .planning/phases/02-batch-processing-reporting/02-CONTEXT.md — D-01 report shape definition
  </read_first>
  <behavior>
    - Test: CLI with valid folder creates report.json in output directory
    - Test: report.json has "total_pills", "total_images", "results" keys
    - Test: report.json total_pills equals sum of counts for "ok" results only
    - Test: report.json total_images equals len(results)
    - Test: Each result has "filename", "count", "status", "centers" keys
    - Test: Error results have "error_msg" key
    - Test: Terminal output still prints per-image lines and summary (D-07)
    - Test: report.json is valid JSON parseable by json.loads
  </behavior>
  <action>
    In `pill_counter/cli.py`:
    1. Add `import json` at top (line 4 area, after existing imports).
    2. Change the process_folder call at line 50 from:
       `results = process_folder(input_path, output_path)`
       to:
       `results = process_folder(input_path, output_path, progress=True)`
    3. After line 53 (`sys.exit(1)` for ValueError) and before the per-image print loop (line 56), add JSON report construction and writing:
       ```python
       # Build JSON report (D-01 shape)
       total_pills = sum(r["count"] for r in results if r["status"] == "ok")
       report = {
           "total_pills": total_pills,
           "total_images": len(results),
           "results": results,
       }
       report_path = output_path / "report.json"
       report_path.write_text(json.dumps(report, indent=2, ensure_ascii=False))
       ```
    4. Keep all existing terminal output code (lines 56-68) unchanged — D-07 requires both terminal and file output.
    5. Keep all existing exit code logic (lines 70-76) unchanged — ERR-03 already implemented.

    In `tests/test_cli.py`:
    6. Add a new test class `TestCLIReport` with tests that invoke `main()` via `patch("sys.argv", ...)` and verify report.json:
       - `test_creates_report_json(tmp_path)`: Create 1 test image, run CLI, assert (out_dir / "report.json").exists().
       - `test_report_has_correct_keys(tmp_path)`: Parse report.json, assert keys are "total_pills", "total_images", "results".
       - `test_report_total_pills(tmp_path)`: Create image with 1 circle, run CLI, assert report["total_pills"] >= 0 (count is valid int).
       - `test_report_total_images_matches_results(tmp_path)`: Create 2 images, run CLI, assert report["total_images"] == len(report["results"]).
       - `test_report_result_has_expected_keys(tmp_path)`: Parse report, check each result has "filename", "count", "status", "centers".
       - `test_report_includes_error_results(tmp_path)`: Create 1 good + 1 corrupt image, run CLI, find error result, assert "error_msg" in it.
       - `test_report_valid_json(tmp_path)`: Run CLI, parse report.json with json.loads — must not raise.
       - `test_terminal_output_still_prints(tmp_path, capsys)`: Run CLI, capture stdout, assert per-image line and "Total:" summary present.

    Do NOT remove or modify the terminal output block (lines 56-68). Do NOT change exit code logic.
  </action>
  <verify>
    <automated>cd /Users/danhtran/myData/src_learn/pill-counter && python -m pytest tests/test_cli.py::TestCLIReport -x -v</automated>
  </verify>
  <acceptance_criteria>
    - CLI calls process_folder with progress=True
    - report.json is written to output_path / "report.json" after processing
    - report.json matches D-01 shape: { total_pills: int, total_images: int, results: [...] }
    - Each result has filename, count, status, centers; error results have error_msg
    - Terminal output (per-image + summary) still prints (D-07)
    - Exit codes unchanged (0/1/2) — ERR-03 preserved
    - All existing CLI tests still pass
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem input | User-supplied folder path — already validated in Phase 1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-02-01 | Tampering | report.json path | accept | Output dir is user-controlled via -o flag; report.json always written to that dir, not arbitrary path |
| T-02-SC | Tampering | tqdm install | accept | tqdm>=4.67.0 already declared in pyproject.toml since Phase 1; no new package introduced |

</threat_model>

<verification>
After both tasks complete, run the full test suite:

```bash
cd /Users/danhtran/myData/src_learn/pill-counter && python -m pytest tests/test_cli.py -v
```

Manual verification:
1. Run `pill-counter tests/fixtures/ -o /tmp/test-output` (or any folder with images)
2. Confirm progress bar appears in terminal
3. Confirm `/tmp/test-output/report.json` exists with correct shape
4. Confirm terminal still shows per-image counts and summary
</verification>

<success_criteria>
- `report.json` created in output directory with D-01 shape: total_pills, total_images, results
- Progress bar displays during batch processing via tqdm
- All existing tests pass unchanged
- New tests cover progress parameter and JSON report output
- Terminal output preserved alongside JSON file (D-07)
</success_criteria>

<output>
Create `.planning/phases/02-batch-processing-reporting/02-01-SUMMARY.md` when done
</output>
